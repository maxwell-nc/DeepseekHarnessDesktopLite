# dsh profile 补丁片段 —— 由 dsh-ui 的插件管理器拼进
# <DSH_HOME>/profiles/<profile>/cordis.patch.yml 的托管块里。
#
# 本片段是一个 YAML 列表，不要写 YAML 文档头。
#
# 这条 patch 把本插件的 host 半边插进 profile 的插件树：
#   * `insert` 不带 `id` 时，新条目直接追加到根条目列表（见 dsh-app-boot 的
#     applyEntryPatches：`const { id, insert, name, ...overrides } = patch`，
#     没有 id 就走 `data.push(...insert)`）。
#   * `name` 是相对 **profile 目录** 的路径 —— 管理器把插件包复制到
#     <profile>/plugins/lan_access/ 之后这个相对路径才成立。
#
# 为什么 `name` 只能写纯字符串：patch loader 拿到 `!!js` 表达式对象时
# name.startsWith 会直接崩（gitbash 插件踩过，见它的 README）。
#
# **刻意不在这里改 webserver 的 host。** 让 dsh 自己监听 0.0.0.0 看起来更省事，
# 但上游把那条路明确堵死了（dsh-web-app 的 startup 里写着「会把 RCE 暴露到网络」），
# 而且那样就没法按来源网段过滤、也没法做「重置访问码」。本插件改成自己在局域网侧
# 另起一个带鉴权的反向代理，dsh 依旧只听回环 —— 详见 lib/proxy.mjs 的文件头。
#
# 浏览器那半边（lib/client.js）不需要在这里声明：dsh 的 client-modules 会
# 按包清单里的 `dsh.client` 字段自动发现它 —— 见同目录 package.json。
- insert:
    - id: dsh-lan-access
      name: ./plugins/lan_access/lan_access.mjs

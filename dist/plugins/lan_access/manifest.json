{
  "id": "lan_access",
  "name": "局域网访问",
  "description": "在侧边栏设置上方加一个入口，点开显示二维码：手机扫码即可在局域网里打开网页版。插件自带一个只放行内网来源、带固定访问码鉴权的反向代理（默认 0.0.0.0:3081 → 127.0.0.1:3080），dsh 自己仍然只监听回环。链接由「内网 IP + 端口 + 固定访问码」组成，IP 不变则链接不变；面板上可一键重置访问码作废旧链接。",
  "version": "1.0.0",
  "order": 30,
  "entry": "lan_access.mjs",
  "patch": "cordis.patch.yml",
  "requiresRestart": true
}
